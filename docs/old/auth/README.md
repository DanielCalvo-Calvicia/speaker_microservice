# API-key auth (removed)

The speaker used to require `SPEAKER_API_KEY` as a bearer token. Auth was an experiment and is not part
of the design (no service authenticates its callers), so it was removed; the last code is kept here.
