# Project

Speaker Microservice: HTTP service that plays a client's raw PCM stream on the local output device.

Intentionally omitted from the Engram tree (add only when needed): `.devcontainer/`, `.github/`,
`k8s/`, `Dockerfile`, `docker-compose.yml`, `domain/entities/` (there is no stateful business
entity: playback state lives in the outbound adapter).
